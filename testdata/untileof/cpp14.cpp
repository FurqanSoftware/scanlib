#include <iostream>
#include <string>

using namespace std;

int main() {
	string cmd;
	while (cin >> cmd) {
		int param;
		if (cmd=="PUSH"||cmd=="REPEAT") {
			cin >> param;
		}
		if (cmd=="REPEAT") {
		}
	}
	
	return 0;
}
