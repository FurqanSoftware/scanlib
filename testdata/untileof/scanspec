var cmd string
for scan cmd
	check regexp.match(cmd, "^(PUSH|POP|PRINT|SIZE|SUM|REPEAT|REVERSE)$")
	var param int
	if cmd == "PUSH" || cmd == "REPEAT"
		scan param
	end
	if cmd == "REPEAT"
		check param > 0, param < math.pow(10, 6)
	end
	eol
end
